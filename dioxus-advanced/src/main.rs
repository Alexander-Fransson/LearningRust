#![allow(non_snake_case)]

use dioxus::prelude::*;
use std::time::Duration;
use async_std::task::sleep;

mod context;
mod conditionalrenders;
mod routhandling;
mod useassets;
mod asyncexample;


fn main() {
    //launch(_);
}

